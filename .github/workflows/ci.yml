name: CI

# Disabled to prevent billing limit failures
# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings
  RUSTDOCFLAGS: -Dwarnings

jobs:
  check:
    name: Check
    if: false  # Ensure job never runs
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled"

  msrv:
    name: MSRV
    if: false  # Ensure job never runs
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled"
