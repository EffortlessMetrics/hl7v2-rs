## 2025-01-28 - CLI Output Readability
**Learning:** For CLI tools, "UX" includes the readability of command output. Raw byte counts are hard to parse for users.
**Action:** Always format file sizes and memory usage into human-readable units (KB, MB, GB). Also, ensure lists of metrics are sorted alphabetically for deterministic and scannable output.
