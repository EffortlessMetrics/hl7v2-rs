# Clippy configuration

# Allow certain lints that are too strict for this project
allow-unwrap-in-tests = true
allow-expect-in-tests = true

# NOTE: The keys `warn-on-all-pedantic` and `allowed-lints` were removed as they are invalid
# for clippy.toml and caused cargo clippy to fail. Pedantic lints should be configured in
# Cargo.toml or passed as compiler flags.
