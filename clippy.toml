# Clippy configuration

# Allow certain lints that are too strict for this project
allow-unwrap-in-tests = true
allow-expect-in-tests = true

# The following keys are invalid in clippy.toml and should be removed:
# warn-on-all-pedantic = true
# allowed-lints = [...]

# Instead, lints should be configured in Cargo.toml or source code.
