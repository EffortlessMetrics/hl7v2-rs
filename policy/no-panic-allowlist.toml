schema_version = "0.3"

# Semantic panic-family allowlist entries belong here when a narrow exception is
# reviewed. Identity is path + family + selector; last_seen is advisory only.
#
# [[allow]]
# path = "crates/example/src/parser.rs"
# family = "unwrap"
# classification = "test_only"
# owner = "parser"
# explanation = "Fixture setup path; covered by follow-up panic-free test helper migration."
# expires = "2026-07-01"
#
# [allow.selector]
# kind = "method_call"
# container = "parses_fixture_with_boundary_case"
# callee = "unwrap"
# receiver_fingerprint = "std::fs::read_to_string(path)"
#
# [allow.last_seen]
# line = 42
# column = 17
